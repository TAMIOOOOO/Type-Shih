version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: typing-speed-challenge-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - JWT_SECRET=${JWT_SECRET:-}
    restart: unless-stopped
